{
  "name": "chip-8-emu",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "start": "yarn build && electron .",
    "build": "cross-env tsc -p ."
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "electron": "3.0.5",
    "typescript": "^3.1.3"
  }
}
